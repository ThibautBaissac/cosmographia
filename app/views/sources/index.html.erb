<%= render("sources/header")%>
<%= render("sources/search", query: @source_query) %>

<div class="container">
  <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
    <%= render(partial: "sources/card", collection: @sources, as: :source)%>
  </div>
  <%= render(PaginationComponent.new(pagy: @pagy)) %>
</div>
