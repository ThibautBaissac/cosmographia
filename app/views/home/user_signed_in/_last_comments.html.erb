<div class="bg-primary bg-opacity-75 mt-5 p-4">
  <div class="container">
    <h2><%= t("home.user_signed_in.last_comments", count: comments.size) %></h2>
    <% comments.each do |comment| %>
      <%= render("my/comment", comment:, user: comment.user) %>
    <% end %>
  </div>
</div>
