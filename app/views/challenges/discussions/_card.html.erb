<%= render(PostComponent.new(
  post: discussion,
  edit_path: edit_challenge_discussion_path(locale, discussion.challenge, discussion),
  destroy_partial: "challenges/discussions/destroy",
  locale: locale,
  current_user: current_user
)) %>
