<footer class="py-4 bg-dark text-light">
  <div class="container d-flex flex-wrap justify-content-between align-items-center">
    <!-- Logo and Copyright -->
    <div class="col-md-4 d-flex align-items-center">
      <%= link_to(root_path, class: "mb-3 me-2 mb-md-0 text-light text-decoration-none lh-1") do %>
        <%= image_tag("logo_inverted.png", alt: "Logo", width: "40", height: "40", class: "d-inline-block align-text-top") %>
      <% end %>
      <span class="mb-3 mb-md-0 ms-2">© <%= Date.current.year %> Cosmographia</span>
    </div>

    <!-- Navigation Links -->
    <ul class="nav col-md-4 justify-content-end list-unstyled d-flex gap-3">
      <% if user_signed_in? %>
        <li>
          <%= link_to(t("feedbacks.feedback"), "#offcanvaFeedback", class: "btn btn-outline-light btn-sm", data: {bs_toggle: "offcanvas"}, role: "button", aria: {controls: "offcanvaFeedback"}) %>
        </li>
      <% end %>
      <li>
        <%= link_to("GitHub", "/cosmographia", class: "btn btn-outline-light btn-sm", target: :_blank) %>
      </li>
    </ul>
  </div>
</footer>
