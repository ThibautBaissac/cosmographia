<div class="container d-flex justify-content-between align-items-center border-bottom pb-3 mb-4">
  <h1 class="my-0 display-5 text-primary"><%= user.fullname %></h1>
  <%= link_to(user_profile_path(locale, user.slug), class: "small text-secondary") do %>
    <span><%= "##{user.slug}" %></span>
  <% end %>
</div>
