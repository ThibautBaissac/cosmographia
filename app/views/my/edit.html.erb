<div class="container">
  <div class=" d-flex justify-content-between align-items-center border-bottom pb-3 mb-4">
    <h1 class="my-0 display-5 text-primary">Edit User</h1>
    <%= link_to(edit_user_registration_path, class: "btn btn-sm btn-outline-primary btn-lg rounded-4 d-flex align-items-center gap-2") do %>
      <i class="fa-regular fa-pen-to-square"></i>
      <%= t("navbar.edit_password") %>
    <% end %>
  </div>

  <%= render("form", user: @user) %>
</div>
