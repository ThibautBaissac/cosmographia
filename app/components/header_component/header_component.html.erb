<div class="d-flex justify-content-between align-items-center pb-3 <%= "mb-4 border-bottom" if border == true %>">
  <h1 class="my-0 display-5 text-primary">
    <%= title %>
  </h1>

  <%= content %>
</div>
