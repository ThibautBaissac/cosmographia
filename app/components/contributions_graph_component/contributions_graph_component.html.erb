<div class="contribution-grid">
  <% @weeks.each do |week| %>
    <div class="week">
      <% week.each do |date| %>
        <% count = @contributions_by_day[date] || 0 %>
        <% level = helpers.contribution_level(count) %>
        <div class="contribution-cell <%= level %>" title="<%= l(date, format: :ymd) %>: <%= count %> contributions">
        </div>
      <% end %>
    </div>
  <% end %>
</div>
